{
  "name": "midp-web",
  "version": "1.0.0",
  "private": true,
  "description": "MIDP web fija",
  "scripts": {
    "start": "node server.js",
    "check": "node --check midp-storage.js && node --check analytics-core.js && node --check analytics-local-data.js && node --check analytics-project-query.js && node --check analytics-echarts-adapter.js && node --check app.js && node --check server.js"
  },
  "dependencies": {
    "echarts": "^6.0.0"
  }
}
